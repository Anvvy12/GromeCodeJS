const getSquareArray = (numbers) => numbers.map((num) => num * num);
export const getAddNumbers = (numbers) =>
  numbers.filter((num) => num % 0 === 1);
export default (a, b) => a + b;

module.export = { getSquareArray };
